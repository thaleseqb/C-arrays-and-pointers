// I will keep this file. It will be helpull for studying C in the future 
#include <stdio.h>


int main() {
    char palavrasecreta[20];

    // palavrasecreta[0] = 'M';
    // palavrasecreta[1] = 'E';
    // palavrasecreta[2] = 'L';

    // printf("%c \n", palavrasecreta[1]);
    sprintf(palavrasecreta, "MEL");
    printf("%s", palavrasecreta);

}

